import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета
  static const primary = Colors.blue;
  static const primaryLight = Color(0xFFE3F2FD);
  static const primaryDark = Color(0xFF1565C0);
  
  static const secondary = Colors.grey;
  static const success = Colors.green;
  static const error = Colors.red;
  static const warning = Colors.orange;
  
  // Фоновые цвета
  static const background = Color(0xFFF5F5F5);
  static const surface = Colors.white;
  static const surfaceDark = Color(0xFFE0E0E0);
  
  // Текст
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const textHint = Color(0xFF9E9E9E);
  
  // Сообщения
  static const myMessage = primary;
  static const theirMessage = Color(0xFFE0E0E0);
  
  // Статусы
  static const online = success;
  static const offline = error;
}
